# Plan: Inspection & Cleanup of the Real-Time Restaurant Project

> **Purpose:** Persist a single source of truth so any agent (or developer) can pick this up, execute the next step, and check it off. Update this file — do not edit tasks in chat — so progress survives across sessions.

> **Rules of engagement**
> - Work through steps **in order** unless marked parallelizable.
> - Mark a step `[x]` only after its verification command(s) pass.
> - Re-run `git status`, `npm run lint`, and `npx tsc --noEmit` after each cluster.
> - **Do not commit** unless explicitly asked.
> - **Do not change the project folder name** — only the `package.json` `name` field.

---

## 0. Reference docs already in the repo

- `AGENTS.md` — project primer, tech stack, architecture conventions, Next.js 16 breaking changes, current roadmap.
- `README.md` — public-facing overview and dev commands.
- `CLAUDE.md` — intentionally a one-liner: `@AGENTS.md` (single source of truth).
- `node_modules/next/dist/docs/` — bundled Next.js 16 docs (especially `01-app/02-guides/upgrading/version-16.md`).

---

## 1. Re-audit & verify the project state [ ]

Read before editing anything. Confirm nothing has changed since this plan was written.

**Files to read:**
- `package.json`, `next.config.ts`, `tsconfig.json`, `eslint.config.mjs`, `postcss.config.mjs`
- `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `app/dashboard/page.tsx`
- `app/features/dashboard/Dashboard.tsx`
- Empty placeholders to confirm still empty: `app/components/{forms,layout,ui}`, `app/contexts/`, `app/features/{auth,settings}`, `app/hooks/`, `app/lib/{api,utils}`, `app/services/`, `app/styles/`, `app/assets/{fonts,images}`
- `.gitignore`

**Deliverable:** a short written confirmation in chat that the state matches `AGENTS.md`'s "Current State & Roadmap" section. If it doesn't, update `AGENTS.md` first.

---

## 2. Clarify with the user [ ]

Before touching code, get answers on these open questions (one round-trip, batched).

1. **`base-design.webp` description** — what does the mockup show? (palette, layout, key components) so docs and UI can match it.
2. **`app/page.tsx`** — full landing/login now, or a "Coming soon" placeholder?
3. **Default `public/*.svg`** — keep or delete? (`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg` — referenced by `Dashboard.tsx` and `page.tsx`.)
4. **Two dashboard stubs** — which model wins: route-side full page (`app/dashboard/page.tsx`), or feature-component composition (`app/features/dashboard/Dashboard.tsx` imported into the route)?
5. **Empty folders** — which get a first real file now, which stay empty until later? (Suggested order: `services/` → `lib/api/` → `lib/utils/` → `hooks/` → `contexts/` → `components/ui/` → …)
6. **`package.json` name** — confirm rename to `real-time-restaurant` is OK (folder name unchanged).

---

## 3. Fix naming/configuration drift [ ]

- [ ] Edit `package.json`: `"name": "real-time-restuarant"` → `"name": "real-time-restaurant"`.
- [ ] Confirm `.gitignore` covers `.next/`, `.next/dev/`, `next-env.d.ts`, `.env*`, `node_modules`. (Verify only.)
- [ ] Confirm Node engine requirement (≥ 20.9.0) is documented in `AGENTS.md` and `README.md`. (Already done.)

---

## 4. Replace boilerplate code [ ]

- [ ] `app/layout.tsx` metadata: `title: "Create Next App"` → `"Real-Time Restaurant"`; `description: "Generated by create next app"` → `"Real-time restaurant management dashboard built with Next.js 16."`
- [ ] `app/page.tsx` — per user answer to question 2: (a) full landing/login, or (b) "Coming soon" placeholder linking to `/dashboard`.
- [ ] Decide on `public/*.svg`s per user answer to question 3.

---

## 5. Reconcile the two dashboard stubs [ ]

Two conflicting stubs exist (both untracked):

| File | Current shape |
| --- | --- |
| `app/dashboard/page.tsx` | Full page: top nav, left sidebar (Overview/Orders/Menu/Staff/Settings), restaurants table |
| `app/features/dashboard/Dashboard.tsx` | Card section: 3 stat cards (Visitors/Sales/Orders), uses default `/window.svg` |

- [ ] Per user answer to question 4, pick **one source of truth**.
- [ ] Move the hard-coded `restaurants` array out of the page into `app/services/restaurants.ts` (stub data, but real wiring).
- [ ] Make the chosen feature component compose cleanly with the route.
- [ ] Verify both files type-check.

---

## 6. Wire the folder scaffolding [ ]

Per user answer to question 5, in this priority order:

- [ ] `app/services/` — restaurant data service + types
- [ ] `app/lib/api/` — base fetch client
- [ ] `app/lib/utils/` — helpers (`cn`, formatters)
- [ ] `app/hooks/` — `useRestaurants` etc.
- [ ] `app/contexts/` — auth + toast providers (Client Components)
- [ ] `app/components/ui/` — `Button`, `Card`, `Input`, `Table`
- [ ] `app/components/forms/` — `LoginForm`
- [ ] `app/components/layout/` — `Sidebar`, `Topbar`, `DashboardShell`
- [ ] `app/features/auth/` — login page + form glue
- [ ] `app/features/settings/` — settings page

Anything not scaffolded stays empty until later. Use the path alias `@/...`.

---

## 7. Documentation updates [ ]

- [x] `AGENTS.md` — full project primer + Next.js 16 notes + roadmap
- [x] `README.md` — replaced create-next-app boilerplate
- [ ] `CLAUDE.md` — confirm `@AGENTS.md` one-liner is intentional. **Decision: leave as-is.**
- [ ] Add a short "Design Reference" subsection to `AGENTS.md` and `README.md` summarizing `base-design.webp` (palette, layout, key components) once the user describes it (Step 2, question 1).

---

## 8. Verify & commit-prep [ ]

- [ ] `npm run lint` — must pass.
- [ ] `npx tsc --noEmit` — must pass.
- [ ] `npm run build` — must succeed (Turbopack by default).
- [ ] `git status` clean of unintended changes; only the planned files modified.
- [ ] **Do not commit.** Wait for explicit user approval.

---

## Progress summary

| Step | Status |
| --- | --- |
| 1. Re-audit | [ ] |
| 2. Clarify with user | [ ] |
| 3. Naming/config drift | [ ] |
| 4. Replace boilerplate | [ ] |
| 5. Reconcile dashboard stubs | [ ] |
| 6. Wire folder scaffolding | [ ] |
| 7. Documentation | [x][x][ ][ ] |
| 8. Verify & commit-prep | [ ] |
